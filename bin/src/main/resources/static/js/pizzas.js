// Exemple simple : aucun JS particulier pour l'instant,
// mais tu peux ajouter des scripts ici.

document.addEventListener('DOMContentLoaded', function() {
    // Par exemple, gestion confirm suppression déjà en inline,
    // ici tu peux rajouter d'autres scripts au besoin.
});
